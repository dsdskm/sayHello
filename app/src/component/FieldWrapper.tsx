import { Paper } from "@mui/material";
import { styled } from "@material-ui/styles";
export const FieldWrapper = styled(Paper)({
  margin: 10,
  padding: 50,
  minWidth: 500,
  textAlign: "center",
});

export const FieldWrapperSmall = styled(Paper)({
  margin: 10,
  padding: 50,
  minWidth: 250,
  textAlign: "center",
});
